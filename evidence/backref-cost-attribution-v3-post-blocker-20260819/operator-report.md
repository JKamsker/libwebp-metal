# Backref cost attribution V3 — supplemental post-blocker audit

## Outcome

The naturally completed rehearsal-refusal return is safe to package. All eight
raw returned files are preserved byte-for-byte and independently indexed. The
existing six-file returned index verifies with zero hash or byte-count
mismatches, and the transfer receipt matches the frozen Phase 1 source
identity.

This supplement does not change Phase 3's consequence: the interval family
remains **unassessed**, `timed_execution_authorized` remains `false`, and no
performance, production, or CPU-boundary claim is available.

## Receipt reconstruction

The return records source commit
`a8a9dfdb3cd882d63980f4234c40e5c8cb91f5e1` and tree
`af7eabc288db9de596d16b1b6a51c38ab25174bf`. The source bundle SHA-256 is
`9febb7b894dcd7e4c9eb7cb5a7475f8160c1459304abbbbb2f5e11e17d7472a7`.
All 19 admitted source hashes match both the frozen V3 identity and the local
files. Repository-context bundle verification passed, the admitted tree was
clean, and the malformed-bundle fixture was rejected without leaving its clone
or verification repository.

The refusal fixture pipeline exited 0 with empty stdout and stderr. Benchmark
lease acquisition was not attempted; no timed binary, encoder workload,
profiler, CUDA path, archive, or extraction ran. No lease receipt was returned
or accepted. There are zero timing rows and zero performance samples.

## Cleanup and absence proof

The independent cleanup receipt is valid and duplicated exactly in the
transfer receipt. It identifies canonical exact child
`/Users/pandocs-agent-001/.libwebp-metal-backref-cost-attribution-v3/run.GafKk1`
under the V3 safe root, with token `run.GafKk1`. It records:

- `validated=true`;
- `removed_exact_validated_child=true`;
- `absence_proved=true`;
- `status=complete`.

This supplemental pass performed no cleanup, remote contact, process
inspection, signaling, deletion, workload, or experiment action. The reported
session `37199` had already completed naturally and was not touched.

## Narrowed return-protocol blocker

The pipeline itself completed successfully. The return wrapper then
unconditionally requested `evidence/lease-record.json` before archive return.
The SCP child waited for the frozen 1,200-second bound and timed out, so archive
return and extraction were never reached. Because no benchmark lease
acquisition was attempted, this lease-record dependency carried no benchmark
safety value.

The receipts establish the SCP timeout and absence of an accepted local lease
receipt. They do not prove physical remote noncreation: the frozen refusal
fixture source writes a no-acquisition placeholder. This precision does not
alter the corrective boundary.

V4 may be proposed only under wholly new independent default-off gates. Its
scope is limited to return protocol: rehearsal/refusal modes must not request
or require a lease record unless benchmark lease acquisition was attempted,
while the independent canonical exact-child cleanup receipt and absence proof
remain mandatory. V4 is not implemented or run here.

## Preserved limitations

The returned refusal receipts do not provide remote benchmark admission,
device/resource timing evidence, a lease, an archive, extracted operator rows,
or any timing sample. They cannot explain the photo end-to-end result, close or
reopen the interval family, rank a CPU boundary, or support production change.

The machine audit is `supplemental-audit.json`; all raw receipt hashes are in
`receipt-index.json`.
