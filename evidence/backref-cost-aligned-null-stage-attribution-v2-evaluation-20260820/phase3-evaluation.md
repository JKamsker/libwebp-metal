# Backref Cost Aligned Null Stage Attribution V2 — Phase 3 Evaluation

## Decision

The graphic-large method 4 drift **remains unaccounted/inconclusive**. This is
not a fresh non-replication and not a localization. Phase 1B authority is
`NONE`, Phase 2 correctly refused all contact and work, and there are zero
fresh V2 timing or stage rows and zero fresh pairs. The frozen primary
reproduction test therefore cannot be applied. There is no stage attribution,
no causal inference from nested overlaps, no H interpretation, no threshold
change, and no production promotion.

The exact blocker is the immutable Phase 1B rehearsal refusal: transfer stopped
after 5 of 364 request chunks, or 327,680 of 23,820,500 bytes. Twenty-one
success receipts are missing, cleanup remains `unknown-after-refusal`, and
exact-child absence proof is false.

The sole future research lead is a new, independently gated Phase 1 that
completes one hashed rehearsal with complete request/response,
archive/integrity/extraction, cleanup footer, and exact-child absence proof
before a later Phase 2 executor reconsiders authority. It was not run.

## Exact ancestry and additions-only handoffs

The independently verified chain is linear and exact:

1. main `fb297dd1097f6fc5a2f2d4d453da3c24b235d7d1`, tree
   `6845d6d64edc8d099c38309953da225bc6291363`;
2. Phase 1A `b9a26a196c65dc56e4c9eaac337c156568e855a5`, parent main, tree
   `907795e6cf91786da93254d3a1bf1678ef49d4ca`;
3. Phase 1B `d72feabc06cdd076ad858400bed8243991a185e7`, parent Phase 1A,
   tree `a48a21becc34d71f5e3a84eb317d23716cd0b244`;
4. Phase 2 `cc5b1b1faec57ab9567d65477b5ea7ccea54a1c5`, parent Phase 1B,
   tree `9319b5848d01ef2c6d439c5245894072336c8b0b`.

Phase 1A changed 51 paths: 44 additions and seven modifications. Phase 1B
contains exactly eight additions and no mutation, deletion, or rename. Phase 2
contains exactly two additions and no mutation, deletion, or rename. Its
execution directory contains only the human and machine refusal audits. The
Phase 1B record's shorter branch label is a metadata anomaly and cannot grant
authority.

Independent SHA-256 checks passed for all 70 manifest-frozen artifacts and all
six Phase 1B indexed files (8,288 bytes), with zero hash or size mismatches.
The admission identity has 71 paths including the manifest. The schema catalog
has protocol version 1 and 47 unique entries; the manifest's 17 schema IDs are
all present. Frozen order is photo then graphic, method 4 then 6, with
`B,L`, `L,B`, `B,L`, `L,B` across repetitions. No V1 or V4 sample was loaded,
pooled, relabeled, reused, or ingested.

Key hashes are:

| Artifact | SHA-256 |
| --- | --- |
| Manifest | `0609dda754e819a2f96d577169435bec3f42af4c5d754e850774cde782eaa396` |
| Protocol | `8d6059a65644c464cba47d17cd34c95f71ddf74d8f5b256b6dfe808818900595` |
| Schema catalog | `61090f0812ecceaa1baf258e53f78918647ab3f8fe9f0f6cd6ade62f757cc055` |
| Phase 1B authority | `45ee92ada3aea5e6e1bae42877c8c2dc1ee9551a471b07e1255c100480e6d474` |
| Phase 2 machine audit | `a5ed2ccd3c5e18d06e3b5f7dbae873a31d0396e67b51478e000afeecfadfbddf` |
| Phase 2 human audit | `00d24f99f611f8b57c67fdfc0bbef5b5349070f0521d28e4cc99c9ae764c7801` |

## V4 and V1 premise isolation

V4 remains premise only. Eleven of twelve domains passed. The sole failure was
graphic-m4 whole encode: mean `-0.6199563389094708%`, paired-bootstrap 95% CI
`[-1.0795343104954225%, -0.21412171812593112%]`. Graphic-m4 DP total and DP
steady passed their null tests. V4 has no complete mutually exclusive
whole-encode partition, so its nested and overlapping timers cannot establish
a stage cause. Phase 3 ingested zero V4 rows into V2 statistics.

V1 independently reconstructs as 13 of 14 gates passing. Its sole failure was
the stale `alignment build frozen` six-body expectation: six private addresses
and section/body/padding `83,212/5,624/77,588` bytes, versus the valid receipt's
four addresses and `50,444/3,600/46,844` bytes. V1 has zero raw timing rows,
zero raw stage rows, zero bootstrap resamples, and contributes zero samples to
V2.

## Canonical four-body identity

Expected and actual receipt files are byte-identical. Each SHA-256 is
`00a47a1e9e4d012ae45f89b173ec16c4c8c63965d15418debd410b288afeba81`.
The generated domain is exactly:

| Ordinal | Variant | Role | Symbol | Address | Size |
| ---: | --- | --- | --- | --- | ---: |
| 0 | B | PushInterval | `PushInterval` | `0000000100064000` | 508 |
| 1 | L | PushInterval | `PushIntervalLayoutClone` | `0000000100068000` | 508 |
| 2 | B | DP | `BackwardReferencesHashChainDistanceOnly` | `000000010006c000` | 1,292 |
| 3 | L | DP | `BackwardReferencesHashChainDistanceOnlyLayoutClone` | `0000000100070000` | 1,292 |

All addresses are distinct and zero modulo 16,384. Entry spacing is 16,384
bytes and corresponding DP-to-Push displacement is 32,768 bytes. B/L semantic
and normalized-work evidence agrees; corresponding canonical instructions,
instruction counts, branch/load/store counts, mnemonic digests, and code sizes
agree. Raw instruction-word digests remain relocation-sensitive and are not
misrepresented as normalized identity.

The executable pure-instruction section is `__TEXT,__br_null_v2`, address
`0x0000000100064000`, alignment power 14, file offset 409,600, flags
`0x80000400`, and size 50,444 bytes. Bodies total 3,600 bytes and inter-entry
padding totals 46,844 bytes. The linker map is 69,697 bytes with SHA-256
`7b5765fcd01505e56a5075fa96fc4779000a656275722565c9be534472d2b142`;
the linker uses `-Wl,-no_deduplicate`. The frozen binary is 643,488 bytes with
SHA-256 `edb1601a77cc3312687fc66fce3d3c65eb9f22fecba359e49a58f4ed45828cb4`.

Untimed negative checks passed: six domain mutations, thirteen receipt
mutations, the V1 stale six-body regression, six payload predecessor digests,
six toolchain/xcrun predecessor identities, twelve streaming fixture cases,
and archive/extraction corruption and malicious-member refusals were all
rejected.

## Local evidence versus dynamic gates

Phase 1's frozen local validation has 47 check fields: 44 pass labels plus the
recorded counts of two local correctness encodes, 24 sanitizer boundary
encodes, and two sanitizer configurations. It records semantic/work/codegen
equality, deterministic bitstreams, selector/body ownership, public API/ABI
equality, ordinary-build default omission, and a recorder-free timed binary.

The local clock fixture has 4,096 reads and a 42 ns p99 consecutive-read delta.
Both timer-accounting rows have 68 actual and expected reads, zero
reconciliation delta, and zero residual. Five early/fault/skip/repeat fixtures
have 2, 4, 4, 6, and 6 reads, matching
`2 + 2 * sum(all emitted stage calls)`. Eight additional untimed Phase 3
fixture suites passed for identity, payload, toolchain, process ownership,
archive/extraction, streaming, admission, and protocol behavior.

These local facts do not substitute for Phase 2 dynamic gates. Dynamic
correctness, timer-ledger reconciliation, direct serial ownership, transport,
source admission, remote payload/toolchain identity, device/resource/process
admission, archive/integrity/extraction, and cleanup/absence are all
`NOT RUN / UNAVAILABLE` under authority `NONE`.

## Rehearsal refusal and Phase 2 zero record

Independent parsing of the Phase 1B channel found one READY frame, one
declaration ACK, and five chunk ACKs. The declaration specifies 23,820,500
bytes in 364 chunks of 65,536 bytes. Only 327,680 bytes were acknowledged;
there is no complete ACK and no response. Channel stdout is 3,941 bytes and
transport stderr is zero bytes. There were 21 missing success receipts and 22
refusal reasons including command exit 1. Cleanup footer verification is
false, cleanup is unknown, and absence proof is false. The rehearsal performed
zero lease attempts, encoder/timed workloads, correctness encodes, timer
children, runner children, work rows, profiler workloads, and performance
samples, with no retry or rerun.

Phase 2 then recorded zero SSH, host/device/runner/remote contact, ping, `nc`,
benchmark, lease, transaction, cleanup, child, correctness, timer, work,
stage, timing, sample, profiler, CUDA, archive/extraction, retry, rerun, and
alternate/local timing actions. It made zero code, protocol, threshold, or
manifest edits.

## Frozen inventory and zero statistical result

| Record | Frozen | Phase 2 observed |
| --- | ---: | ---: |
| Direct runner children | 59 | 0 |
| Correctness encodes | 16 | 0 |
| Clock children / reads | 1 / 4,096 | 0 / 0 |
| Timer-accounting children / rows | 2 / 2 | 0 / 0 |
| Untimed work rows | 8 | 0 |
| Profile processes | 32 | 0 |
| Excluded warmups | 32 | 0 |
| Retained timing rows | 128 | 0 |
| Runner rows | 160 | 0 |
| Stage rows | 160 | 0 |
| Retained stage rows | 128 | 0 |
| Fresh pairs per cell/metric | 16 | 0 |
| Bootstrap resamples per statistic | 100,000 | 0 executed |

The frozen reporting schema contains whole encode; eight mutually exclusive
measured top-level buckets; computed residual; and seven descriptive,
non-additive nested timers. The raw ledger requires eleven timer-stage fields.
Across four cells this would yield 68 paired reported statistics, but zero are
computable.

Therefore whole encode, every top-level bucket, residual, and every nested
stage have pair count zero. No timing shares, stage deltas, means, medians,
sample standard deviations, extrema, signs, repeatability ratios, share ranges,
or confidence intervals can be computed. No paired statistic or bootstrap
result was fabricated.

## Frozen decision tree and consequence

The primary graphic-m4 whole-encode test requires 16 valid fresh pairs before
the unchanged null bounds can classify reproduction. With zero pairs, it
cannot pass or fail. The exact result is
`INCONCLUSIVE_REFUSED_AUTHORITY_NO_FRESH_REPRODUCTION_NO_STAGE_ATTRIBUTION`.
The attribution branch is not entered, and graphic-m4 remains
`REMAINS_UNACCOUNTED_INCONCLUSIVE`.
