# Backref cost attribution V6 — Phase 3 independent consequence report

## Decision

This cycle is **unassessed**. It neither closes nor reopens the interval
family, does not select or implement a new bounded experiment, does not rank a
next CPU stage, and does not authorize production promotion. No V6 timed rows
exist. Therefore every requested performance median, share, delta, and
repeatability result is N/A; a zero row count is not a zero duration.

The exact blocking reason is the sole permitted untimed no-workload rehearsal's
SSH timeout while attempting cleanup of the exact validated canonical child
`/Users/pandocs-agent-001/.libwebp-metal-backref-cost-attribution-v6/run.etNrsc`.
It returned neither independent cleanup/absence proof nor transport receipts.
The committed envelope consequently records `authorization_status=REFUSED`,
`timed_execution_authorized=false`, and `phase_2_authority=NONE`. Phase 2 had
no authority to invoke the frozen run command and correctly performed no
timed, remote, retry, rerun, fallback, profiler, CUDA, lease, or encoder action.

Production disposition is an explicit **NO-GO**. The diagnostic remains
default-off and diagnostic-only; no result in this cycle supports promotion.

## Evidence inventory and raw-row reconstruction

The complete committed V6 evidence inventory before this evaluation contains
four files totaling 6,468 bytes:

| Evidence | Bytes | Consequence |
|---|---:|---|
| `phase1-local-validation.json` | 2,372 | frozen local design validation; no Phase-2 sample |
| `phase1b-authorization.json` | 1,943 | explicit refusal and no Phase-2 authority |
| `phase1b-rehearsal-hashes.json` | 381 | indexes the sole raw refusal file |
| `rehearsal/rehearsal-invocation.json` | 1,772 | one SSH cleanup timeout; zero workloads |

There is no
`evidence/backref-cost-attribution-v6-execution-20260819` directory. There are
zero V6 JSONL files, runner rows, stage rows, retained rows, warmups,
correctness receipts, timer-accounting receipts, resource receipts, command
rows, lease receipts, archives, or performance samples. Older experiment rows
were not pooled, relabeled, or reused.

For each of photo-large/method 4, photo-large/method 6,
graphic-large/method 4, and graphic-large/method 6, both baseline and candidate
raw-row counts are zero. Whole encode, lossless analyze, hash chain, backward
references, selected-DP total/setup/steady, traceback, materialization,
histogram, Huffman, and bitstream/downstream values are all N/A. Stage shares,
baseline-to-candidate deltas, medians, and repeatability are likewise N/A.

## Ancestry and immutable identity

The independently resolved local lineage is exact and linear:

1. frozen main `c9d21ac9c512c94f036fc2db7eb118da83b4f7f9`, tree
   `0537b71b0915a5b3718c0155c351511e0cb60c64`;
2. Phase 1A `f3e0aa9bf9ac1dfefaf2687743387838b7d204db`, direct parent main,
   tree `dfb525f81979cd1ff5992a1d1f4c4865807812d2`;
3. Phase 1B `20359ce80173d72a8c2766926f3a29f23f177854`, direct parent Phase
   1A, tree `b766e3ec274ab635e354d50f030b7e8d44354d6f`.

The Phase 1A handoff identifier supplied to this evaluator ends in `db2`, is 41
hex characters, and does not resolve as a Git object. The repository and every
committed V6 envelope/receipt field consistently identify the actual 40-hex
Phase 1A commit ending in `db`. This handoff-text defect does not create
authority and is recorded rather than silently accepted.

Phase 1A changed 24 files (4,669 insertions, 22 deletions). Phase 1B changed
exactly three files (78 insertions), all additions at the predeclared raw
rehearsal, hash-index, and authorization-envelope paths. It modified no Phase
1A file.

The immutable set is the manifest plus its 26 enumerated frozen artifacts: 27
artifacts total. All 27 are byte-identical at Phase 1A and Phase 1B, and all 27
independently recomputed SHA-256 identities match their applicable manifest or
envelope declarations. Core identities are:

- manifest `cb3dbeb9f6cb60b7c0100e8a25a5c5459da291edca1e87e90d41e98b65f022e9`;
- protocol `84134ae5725615122b46022465b166ebe1a3d62d3cbd974af272e66b74e973e7`;
- schema catalog `5177ad4a5c144b50dd26a71672dbc5a59ced7577f0f709377c6f2837ce0da2ea`;
- Phase-1 local validation `200e77e0b7fdd127f356849de90b143f6ea90cf49bb2b675342de7f843d05a24`;
- rehearsal hash index `ee221e70e43c8505118a7f83f572163e8d304120c3d710d2e686c65d8090a3cf`;
- sole raw refusal `5c6a71df1947fc330592e851ba0a3b4bac324094f5e1e31f218fe6ab45ec2d99`.

## Protocol, semantics, order, counts, and timers

Source and preprocessing inspection confirm fresh default-off V6 build,
compile, and exact-value runtime gates. The candidate is enabled only by exact
`WEBP_BACKREF_COST_ATTRIBUTION_V6_EXPERIMENT=1`. Ordinary objects omit V6,
predecessor gates are isolated, and local public API/ABI surfaces match.

The specialized `PushInterval` body is whitespace-normalized identical to the
frozen `673bc3fa` source. V6 evaluates its selector once before DP mutation and
uses one closed baseline/candidate `if/else`; the predecessor fallthrough chain
is excluded by preprocessing. Baseline and candidate instantiate the same DP
body with only the frozen `PushInterval` implementation selection and private
counter identity differing. Local counter checks observed only the selected
body, equal encoded bytes, and explicit zero reset.

The timer source boundaries match the frozen contract: DP total surrounds
exactly one selected DP call through cleanup; setup starts before allocation
and ends after successful `CostManagerInit`; steady covers the initial literal
and pixel loop only; traceback and materialization are separate; no clock is in
`PushInterval`, insertion, or another per-pixel helper. The retained-row gate
requires selected body/selector/DP total/setup/steady call equality,
`setup+steady <= DP total`, and
`DP total+traceback+materialize <= backward references`.

The frozen plan is photo-large then graphic-large, method 4 then method 6, with
three paired repetitions ordered baseline/candidate, candidate/baseline,
baseline/candidate. It permits 24 serial directly owned profile processes, 24
excluded warmups, 96 retained rows, 120 runner rows, and 120 stage rows after
16 correctness encodes and eight counter-check encodes. Actual Phase-2 counts
for every item are zero. No order or count was realized and none may be
inferred from the plan.

The local clock check read `mach_continuous_time` 4,096 times and passed
monotonicity with 14 ns mean read delta and 83 ns maximum delta. This is an
untimed local timer-overhead invariant, not a case/method performance sample.

## Source, toolchain, device, resources, ownership, and return

The manifest freezes Darwin arm64, Mac16,10 / Apple M4, 24 GiB, Apple clang
17.0.0, SDK 26.2, AC power, Low Power Mode off, no thermal warning, at least 4
GiB available memory, at least 8 GiB disk, one exclusive lease, and one serial
direct child. No exact-Phase-1A source-admission receipt, remote toolchain/build
identity, target device/resource receipt, lease receipt, correctness receipt,
process-order receipt, or timer receipt returned. Those realized states are
therefore unassessed; frozen declarations and local tests are not substitutes.

Static and local ownership tests pass: process discovery is refusal-only, no
discovered PID can enter the signal API, and only a retained directly spawned
`Popen` handle can be stopped after timeout. The raw refusal reports zero
discovered-PID signals, zero encoder/profiler workloads, and no lease attempt.
Phase 2 performed no discovery, spawn, or signal action.

The two-plane design and local fixtures pass compact-control, three-state
lease, archive size/hash/inventory, safe-member, all-or-nothing extraction,
quarantine, cleanup-last, and exact-child containment checks. The actual
rehearsal returned none of the five required receipts:

- `transfer-cleanup.json`;
- `rehearsal.json`;
- `return-protocol/source-admission-receipt.json`;
- `return-protocol/operator-status.json`;
- `return-protocol/cleanup-receipt.json`.

Observed transport calibration is `NOT_OBSERVED`: zero archive bytes, zero
chunks, zero elapsed nanoseconds, and no hash or throughput observation. The
cleanup attempt timed out before contact, so the exact remote child's present
absence is unknown. No cleanup-success or absence claim is made, and the
explicit no-contact/no-retry rule was preserved.

## Local Phase-3 validation and consequence

Untimed local protocol, admission, archive, ownership, experiment-guard, and
correctness/sanitizer suites pass. The latter performed 16 local correctness
encodes, four counter cells, two sanitizer configurations, and 16 boundary
fixtures; these establish local structural and correctness properties only.
The executor's local `validate` command refuses with exit 1 because
`rehearsal/transfer-cleanup.json` is absent, confirming fail-closed handoff
behavior without remote contact.

The consequence is final for this cycle: **interval family unassessed, no next
CPU-stage ranking, no new experiment, and no production promotion**. Any
future work would require separately granted authority and wholly new gates;
this Phase 3 neither performs nor authorizes it.

The machine-readable reconstruction is in `phase3-audit.json`; local command
outcomes are in `phase3-local-validation.json`.
