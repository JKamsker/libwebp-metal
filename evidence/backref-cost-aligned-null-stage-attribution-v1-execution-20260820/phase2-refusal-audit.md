# Phase 2 frozen executor/auditor refusal

BACKREF COST ALIGNED NULL STAGE ATTRIBUTION V1 Phase 2 is refused fail-closed. The exact audited chain is main `5de7b3848adfdb5d1058a061979535f05f976e5b` → Phase 1A `c674c815c65d0424f75f8391e09064da4364d04e` → Phase 1B `75ba4d7f001307becee11828f321f9f041ad64a6`, with the required single-parent ancestry and no Phase 1A file modified by Phase 1B. Phase 1B contains 29 additions and zero modifications/deletions/renames.

The frozen Phase 1B authorization envelope is `REFUSED` with conditional authority `NONE`. Its 14 gate outcomes are 13 true and one false: `alignment build frozen`. The Phase 1B recorder gate at `scripts/record_backref_cost_aligned_null_stage_attribution_v1_phase1b.py:201-214` still requires six private aligned bodies, a 83,212-byte section, 5,624 body bytes, and 77,588 padding bytes. The valid V1 receipt records four distinct private B/L bodies, section `__TEXT,__br_null_v1` size 50,444 bytes, 3,600 body bytes, 46,844 padding bytes, 16,384-byte spacing, and 32,768-byte DP-to-Push displacement. This mismatch is the sole refusal reason. It is preserved as evidence; it is not overridden, repaired, retried, or reinterpreted.

The independent local audit verified the frozen identities and receipts: manifest SHA-256 `b38602331821dc2e6f17d1aa82e974891697109e9d9011c724173bf55ae26439`, protocol SHA-256 `4dc75a93005fba3a85ccb8eb353ad9c944d379d9603d9c0a75b05fde4033196d`, schema catalog SHA-256 `e32aea4c674690431b470944476efe9abce959fe89d3d5e02f618246a10b7156`, and transport-source SHA-256 `d295ae6980dc13aabf08b4eca42bfdc288eb571b96bf292413bb0a98f13bf46c`. All 27 Phase 1B rehearsal files and 364,350 bytes match the frozen hash index; the nested indexes verify 9/9 artifact entries and 25/25 returned-evidence entries. The 66-entry source map has zero file-hash mismatches and exactly matches the manifest path set. The schema catalog has 45 unique entries, and all 17 manifest schema IDs are present.

The prior Alignment V4 audit is premise-only: 11/12 domains passed, with the sole graphic-m4 whole-encode failure mean `-0.6199563389094708%` and 95% CI `[-1.0795343104954225%, -0.21412171812593112%]`; graphic-m4 DP-total and DP-steady controls passed. Zero V4 timing rows were loaded into V1 statistics, and V4’s nested timers were classified as non-additive descriptive evidence.

Phase 1’s fresh V1 static/fixture audit remains recorded as PASS: 43 named checks, 45 experiment guards, CMake and Unix-make integration, default-off/API/ABI isolation, separate recorder and recorder-free timed binary, B/L-only runtime truth table with H invalid/absent, 2 local correctness encodes, 24 sanitizer boundary encodes in 2 configurations, 5 early/fault/skip/repeat fixtures, and 4-body B/L codegen/alignment equality. The complete ledger freeze has 9 top-level buckets including residual, 11 nested descriptive timers that are never summed with top-level buckets, `mach_continuous_time`, the frozen clock-read formula, independent 4,096-read overhead measurement, zero-residual reconciliation, and frozen primary/attribution thresholds. These are static and fixture results only; no Phase 2 dynamic rows exist.

The existing Phase 1 rehearsal is referenced only, not counted as Phase 2 execution. It used one remote session, 362/362 acknowledged request chunks, 23,673,635 request bytes, 18,240 response bytes, a 13,846-byte archive, exit 0, zero stderr, and exact-child cleanup/absence. It had zero lease attempts, encoder workloads, correctness encodes, work rows, timer-validation children, direct runner children, profilers, and timing samples. The Phase 1 raw remote contact is therefore distinct from this Phase 2 result.

Phase 2 made zero host or remote contact: zero SSH, READY, lease, remote transaction, ping, `nc`, workload, profiler, CUDA, remote child, remote cleanup, archive/extraction, correctness, work, timer, stage, and timing operations. There are zero Phase 2 samples and zero timing rows. Only local read-only Git, filesystem, hashing, JSON, and source/schema inspection was performed.

Decision: `REFUSED_ZERO_ACCEPTED_PERFORMANCE_EVIDENCE`. No whole-encode reproduction, stage attribution, nested-timer statistic, or production interpretation is available. The blocker is the immutable Phase 1B alignment gate mismatch; Phase 2 has no next action and does not select a consequence lead.

Machine-readable evidence: `phase2-refusal-audit.json`.
